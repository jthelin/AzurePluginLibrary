A plugin which restarts a Web or Worker Role after a random period of time.

The average time before a restart is requested can be configured as a setting:

Two10.ChaosMonkey.AverageHoursBeforeReboot

Not advised for use in production!